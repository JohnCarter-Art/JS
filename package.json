{
  "name": "js",
  "source": "Baton/src/index.html",
  "scripts": {
    "dev": "yarn run parcel Baton/src/index.html --out-dir dist/dev",
    "build": "yarn run parcel build Baton/src/**.html --out-dir dist/build"
  },
  "devDependencies": {
    "parcel": "^2.4.1"
  }
}
